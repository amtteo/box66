/** Minimálna hodnota platených položiek (bez donášky) pre uplatnenie odmien. */
export const LOYALTY_MIN_PAID_SUBTOTAL = 5;

/** Max počet odmení zobrazených v gride košíka (3×3). */
export const LOYALTY_MAX_GRID_ITEMS = 9;

/** Koľko bodov za 1 € subtotal (floor). */
export const LOYALTY_POINTS_PER_EURO = 1;
